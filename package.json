{
  "name": "exposure-radar",
  "version": "0.1.0",
  "private": true,
  "description": "ExposureRadar monorepo (backend + frontend) for portfolio and local-only execution.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "npm --workspace backend run lint",
    "lint:frontend": "npm --workspace frontend run lint",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --workspace backend test",
    "test:frontend": "npm --workspace frontend test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
