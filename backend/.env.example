# ExposureRadar backend – example environment variables
# Copy this file to ".env" and adjust values for your local environment.

# HTTP server
PORT=4000
NODE_ENV=development

# PostgreSQL connection
# For local development without Docker, you can point to localhost.
# Docker Compose will override this to point to the "db" service.
DATABASE_URL=postgresql://exposure_radar:exposure_radar@localhost:5432/exposure_radar?schema=public

# Redis connection
REDIS_URL=redis://localhost:6379

# Auth / security
JWT_SECRET=dev-only-jwt-secret-change-me
JWT_EXPIRES_IN=1h
